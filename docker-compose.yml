version: '3.5'
services:
  ibdiscord_web:
    build: .
    env_file: details.env
    restart: always
    depends_on:
     - redis
    networks: 
     - web-net_main
  redis:
    image: redis
    networks:
     - web-net_main
networks:
  web-net_main:
    external: true
